.main
  -#%a{ name: product.alias_name }
  - if product.images.first.try(:file)
    = link_to simple_image(product.images.first.file.normal, nil, class: 'pics'), product

  %h4= link_to product.name, product
  %p.text
    = simple_format product.description
  = link_to t.helpers.links.more, product, class: 'all'
  = link_to t.links.fast_order, new_fast_order_path, class: 'fast_order gallery'
  .price= number_to_currency(product.price, locale: 'ru')
