%h2= t '.title'

= form_for(resource, as: resource_name, url: password_path(resource_name)) do |f|
  = devise_error_messages!

  %div
    = f.label :email
    %br
    = f.email_field :email

  %div= f.submit t('.send_reset_password')

= render 'devise/shared/links'
