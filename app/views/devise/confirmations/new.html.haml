%h2= t '.title'

= form_for(resource, as: resource_name, url: confirmation_path(resource_name)) do |f|
  = devise_error_messages!

  %div
    = f.label :email
    %br
    = f.email_field :email

  %div= f.submit t.actions.do_send

= render 'devise/shared/links'
